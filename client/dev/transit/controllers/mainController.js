;(function(ng) {
  'use strict';

  ng.module('public-transport')
    .controller('mainController', [
      function() {
        var self = this;

        return self;
      }
    ]);
}(window.angular));
